import Foundation
import ComposableArchitecture

@Reducer
struct {{ name }}Feature {
	@ObservableState
	struct State {}

	enum Action {}

	var body: some ReducerOf<Self> {
		Reduce { state, action in
			return .none
		}
	}
}
